import {styled} from '@stitches/react';

const StyledTitle = styled('h1', {
    fontSize: '24px',
    color: 'royalblue',
    fontWeight: 'bold',
    marginBottom: '10px',
    });

export default StyledTitle;